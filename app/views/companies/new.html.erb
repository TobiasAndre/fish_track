<div class="max-w-xl mx-auto">
  <h1 class="text-2xl font-semibold text-gray-900 dark:text-gray-100">Criar empresa</h1>
  <p class="mt-2 text-sm text-gray-600 dark:text-gray-300">
    Vamos começar criando a empresa para organizar unidades, açudes e lotes.
  </p>

  <%= form_with model: @company, class: "mt-6 space-y-4" do |f| %>
    <%= render "shared/form_errors", resource: @company %>

    <div>
      <%= f.label :name, "Nome da empresa", class: "block text-sm font-medium text-gray-700 dark:text-gray-200 mb-1" %>
      <%= f.text_field :name, class: "w-full rounded-lg border px-3 py-2 text-sm
                                      bg-white dark:bg-gray-950
                                      border-gray-300 dark:border-gray-700
                                      text-gray-900 dark:text-gray-100
                                      focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
    </div>

    <div>
      <%= f.submit "Salvar", class: "w-full bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400
                                    text-white font-medium py-2.5 rounded-lg transition" %>
    </div>
  <% end %>
</div>
