<div class="max-w-4xl mx-auto space-y-4">
  <div>
    <h1 class="text-2xl font-semibold text-gray-900 dark:text-gray-100">Editar perfil de acesso</h1>
    <p class="mt-1 text-sm text-gray-600 dark:text-gray-300"><%= @profile.name %></p>
  </div>

  <%= render "form", profile: @profile %>
</div>
